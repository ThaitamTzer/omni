# - Muốn ưu tiên hoàn thiện và thử nghiệm giao diện bằng mock data trước khi kết nối/tích hợp dữ liệu và dịch vụ thật; trong quá trình làm UI, muốn đồng bộ layout/design system trước rồi mới đổ mock data. Confidence: 0.98
- Muốn ưu tiên hoàn thiện và thử nghiệm giao diện bằng mock data trước khi kết nối/tích hợp dữ liệu và dịch vụ thật; trong quá trình làm UI, muốn đồng bộ layout/design system trước rồi mới đổ mock data. Confidence: 0.98
- Muốn cấu hình/kết nối Facebook Page qua giao diện ứng dụng, không đưa thông tin Page vào biến môi trường; kết nối thủ công (nhập Page ID, tên và token cho từng Page) phải là luồng chính, không ưu tiên cơ chế tự động phát hiện/lấy Page; hệ thống cần hỗ trợ thêm và quản lý nhiều Page. Confidence: 0.99
- Với các token xác thực khi cấu hình tích hợp, muốn hệ thống tự động tạo token an toàn khi thêm mới và cung cấp nút sao chép để người dùng dễ dùng token cho thiết lập bên ngoài. Confidence: 0.98
- Ưu tiên hoàn thiện các nền tảng bảo mật/xác thực (như refresh token) trước khi chuyển sang các hạng mục tính năng khác. Confidence: 0.94
- Khi phát hiện điểm yếu bảo mật, muốn khắc phục triệt để ở cả frontend, backend và realtime — không chỉ đánh giá hiện trạng — đồng thời kiểm thử các trường hợp được phép và bị từ chối trước khi kết luận hoàn tất. Confidence: 0.97
- Muốn frontend dùng TanStack Query cho data fetching/cache và Zustand cho quản lý state thay vì tự viết wrapper/state context. Confidence: 0.99
- Muốn các cấu hình theo từng tích hợp/tính năng (như Facebook Pages) được tách thành trang quản lý riêng, có mục truy cập trực tiếp trên sidebar thay vì gộp chung vào trang Cài đặt. Confidence: 0.97
- Muốn sidebar luôn có trạng thái active rõ ràng cho menu hiện tại, bao gồm cả các route con, bằng cách làm nổi bật trực quan mục đang chọn. Confidence: 0.95
- Ưu tiên dùng Material UI (MUI) làm thư viện component/design system cho dashboard React để có giao diện admin đồng bộ và nhiều component dựng sẵn. Confidence: 0.9
- Muốn đồng bộ giao diện giữa các trang dashboard theo cùng một pattern thị giác: header hero nền primary màu xanh dương, avatar chứa icon lớn, nút trắng trên header, card trắng bo góc có border, và empty state với icon lớn. Confidence: 0.98
- Muốn toàn bộ nội dung chữ trên UI được quản lý tập trung qua i18n thay vì hardcode, nhưng hiện chỉ sử dụng tiếng Việt; không cần chức năng chuyển đổi ngôn ngữ hay duy trì locale đa ngôn ngữ. Confidence: 1.0
- Với i18next, mọi chuỗi có biến phải dùng đúng cú pháp interpolation `{{var}}` (hai cặp ngoặc) — UI không bao giờ được hiển thị placeholder thô như "Đã chọn {count}"; khi phát hiện placeholder hiện nguyên văn, phải sửa triệt để toàn bộ các key bị ảnh hưởng chứ không chỉ một chỗ. Confidence: 0.8
- Muốn tách rõ cấu hình môi trường theo app: API đọc root `.env` qua NestJS ConfigModule, còn web chỉ dùng `.env` trong `apps/web/` với biến có tiền tố `VITE_`; endpoint API của web mặc định cấu hình qua proxy trong `vite.config.ts`, không dùng các biến root như `API_URL` hay secret backend. Confidence: 0.98
- Muốn frontend có thể cấu hình độc lập endpoint REST API và WebSocket/Socket.IO qua hai biến môi trường local riêng (`VITE_API_URL` và `VITE_WS_URL`), có fallback hợp lý về proxy hoặc same-origin khi không khai báo; cấu hình Vite/proxy cũng phải đọc và sử dụng các biến này để đồng bộ toàn bộ luồng dev. Confidence: 0.98
- Giao tiếp với assistant hoàn toàn bằng tiếng Việt và kỳ vọng phản hồi/hướng dẫn bằng tiếng Việt. Confidence: 0.95
- Khi hỏi về hành vi/luồng xử lý của hệ thống (ví dụ "khi có tin nhắn đến AI xử lý thế nào"), muốn câu trả lời liệt kê ĐẦY ĐỦ tất cả các trường hợp — bao gồm cả edge case (echo, hội thoại bị xóa mềm/tắt AI, attachments, rate limit, retry BullMQ, thiếu API key) chứ không chỉ tóm tắt luồng chính — người dùng nhấn mạnh "liệt kê hết ra đi". Confidence: 0.6
- Khi gặp xung đột cổng giữa container Docker của dự án và service hệ thống (ví dụ Postgres host chiếm 5432), không muốn đụng tới service hệ thống hay chạy lệnh sudo/disable — ưu tiên giải pháp ít xâm lấn nhất là đổi port container trong docker-compose và cập nhật DATABASE_URL tương ứng. Confidence: 0.85
- Muốn tính năng xóa triển khai theo mô hình hai cấp: xóa mềm đưa dữ liệu vào thùng rác riêng (khôi phục được, không cần xác nhận) và xóa vĩnh viễn phải qua dialog xác nhận rõ ràng, xóa sạch dữ liệu liên quan (cascade) khỏi DB. Confidence: 0.8
- Ưu tiên pattern "thùng rác" riêng (tab Đã xóa, từ đó khôi phục hoặc xóa vĩnh viễn từng mục) hơn là snackbar hoàn tác tạm thời cho thao tác xóa. Confidence: 0.7
- Với hệ thống chat/CSKH, không bao giờ để bỏ lỡ tin của khách: hội thoại bị xóa mềm phải tự khôi phục về inbox khi khách gửi tin mới. Confidence: 0.75
- Không muốn thêm phân quyền/guard theo role (như RolesGuard) khi tính năng không thực sự cần — mọi nhân viên đăng nhập đều được phép thao tác; giữ authorization đơn giản. Confidence: 0.6
- Muốn các tab giao diện có khả năng cuộn ngang khi tràn khung chứa (dùng MUI Tabs `variant="scrollable"` + `scrollButtons="auto"`, kèm `allowScrollButtonsMobile`) thay vì để tab bị tràn/co lại. Confidence: 0.65
- Muốn tính năng realtime (tin nhắn, hội thoại mới) tự cập nhật giao diện ngay khi có sự kiện qua Socket.IO — không được để trạng thái "phải refresh trang mới thấy dữ liệu mới". Confidence: 0.9
- Muốn các thao tác chính trên từng mục danh sách (như xóa mềm hội thoại, khôi phục, xóa vĩnh viễn) có sẵn ngay trên item trong list (nút hover hiện ra) thay vì phải bấm mở chi tiết mới thấy nút thao tác — đã lặp lại ở cả luồng xóa mềm lẫn xóa vĩnh viễn/thùng rác. Confidence: 0.92
- Muốn danh sách (inbox, thùng rác) hỗ trợ chọn nhiều mục và thao tác hàng loạt: checkbox trên từng item (bấm checkbox không mở chi tiết), checkbox "Chọn tất cả" có trạng thái indeterminate, thanh action nổi hiển thị khi có chọn (đếm số đã chọn, nút xóa mềm/khôi phục/xóa vĩnh viễn, nút bỏ chọn), chuyển tab tự clear selection; xóa vĩnh viễn hàng loạt vẫn phải qua dialog xác nhận kèm số lượng — khi thiếu UI/UX cho luồng hàng loạt, muốn bổ sung đầy đủ (backend endpoint bulk + frontend) chứ không bỏ sót. Confidence: 0.85
- Khi kiểm tra một field/dữ liệu (ví dụ tên người gửi tin nhắn), muốn trace xuyên suốt cả chuỗi dữ liệu: xác minh backend/API có lấy được dữ liệu đó không, rồi mới kiểm tra frontend/UI có thực sự hiển thị đúng field API trả về chưa — kết luận phải dựa trên kiểm tra cả hai đầu (API → UI) chứ không phán đoán. Confidence: 0.75
- Không muốn UI/API dùng tên tự sinh giả định (như "Khách {4 số cuối}") thay cho danh tính thật của người gửi — khi khách gửi tin, muốn lấy và hiển thị tên thật (qua nguồn như Facebook Graph API) thay vì tên placeholder sinh tự động — đã xác minh Graph API trả về tên thật và mong muốn triển khai việc lấy tên đó; sau đó người dùng đã xác nhận triển khai luôn. Confidence: 0.85
- Khi cần triển khai tính năng, muốn làm theo quy trình chắc chắn (cho chắc kèo): khảo sát và viết spec/design plan trước (vào plan mode), rồi triển khai từng bước bám theo todo list — không triển khai vội; đây là nguyên tắc áp dụng cho mọi lần cần triển khai (đã áp dụng lại khi redesign toàn bộ UI, kèm hỏi người dùng chốt hướng thiết kế trước khi viết plan). Confidence: 0.95
- Sau khi triển khai tính năng, bắt buộc phải tự verify trên trình duyệt/internet thực tế (mở app đang chạy, đăng nhập, kiểm tra UI hiển thị đúng dữ liệu, chụp screenshot xác nhận) chứ không chỉ test API/build/test suite — người dùng đã nhắc rõ "nhớ là phải tự verify trên internet nữa nha" sau khi chỉ thấy verify qua API. Confidence: 0.92
- Khi verify trên browser, muốn kiểm tra cả các khía cạnh sau: console/network không có lỗi 4xx/5xx, hình ảnh/dữ liệu thật hiển thị (không còn placeholder), và luồng realtime tự cập nhật UI ngay khi có sự kiện mới mà không cần refresh trang. Confidence: 0.8
- Khi áp một thiết kế tham chiếu (ví dụ ảnh dashboard từ Dribbble), muốn bám sát chi tiết tham chiếu — khi người dùng phản hồi "chưa giống", phải phân tích lại kỹ ảnh tham chiếu bằng vision (tập trung đúng phần được nhắc, ví dụ sidebar) rồi mới chỉnh sửa cụ thể, không đoán mò. Confidence: 0.85
- Phong cách sidebar dashboard ưa chuộng (theo tham chiếu Donezo): sidebar hẹp ~180-200px, chia menu thành nhóm riêng — nhóm chính (Hộp thư, Facebook Pages) và nhóm "General" tách phía dưới (Cài đặt, Logout), user info (avatar + tên + logout) đặt ở đáy sidebar; người dùng đã phản hồi sidebar "chưa giống" khi thiếu các yếu tố này. Confidence: 0.7
- Muốn giao diện doanh nghiệp gọn gàng, không mang cảm giác "app AI mẫu": góc bo lớn (12-24px) bị coi là quá cong — giảm về 6px chuẩn (8px tối đa cho dialog/hero). Confidence: 0.85
- Không thêm thành phần UI trang trí không có chức năng thật: ô tìm kiếm giả trong header đã bị yêu cầu bỏ đi ("bỏ cái tìm kiếm hội thoại đi") — tránh các element chỉ để làm đẹp mà không hoạt động. Confidence: 0.8
- Tránh các anti-pattern thị giác: logo emoji (💬), gradient trên login, bong bóng tin nhắn màu đậm + bóng đổ, hover nền tím — tin nhắn dùng row nền nhạt mỏng theo loại người gửi (customer/staff/AI) không shadow; Inbox bố cục 2 cột liền full height ngăn bằng border thay vì 2 card floating bo góc. Confidence: 0.75
- Không bao giờ commit credential thật vào git: file .env.example phải chỉ chứa placeholder (không DATABASE_URL thật), thêm `**/.env.example` vào .gitignore; nếu secret lỡ lọt vào commit chưa push, phải làm sạch khỏi lịch sử trước khi đẩy lên remote. Confidence: 0.7
- Các phần tử UI phải có độ tương phản rõ ràng so với nền — khi màu nền của tin nhắn quá nhạt, trùng với màu background, người dùng phản hồi "các tin nhắn đang có màu giống với màu background quá"; phải tăng độ tương phản theo loại người gửi (customer/staff/AI) kèm border 1px mỏng để tách ranh giới với nền, vẫn giữ phong cách enterprise gọn nhẹ. Confidence: 0.8
- Tin nhắn và các phần tử trạng thái AI trong giao diện chat/inbox phải phân biệt rõ người gửi với người nhận — người dùng phản hồi "màu của tin nhắn cũng chưa phân biệt người gửi với người nhận" và báo lỗi "AI đang trả lời" đang hiển thị bên phía tin khách; cần phân biệt cả hướng (khách bên trái, nhân viên/AI bên phải — kể cả indicator typing của AI) lẫn màu nền riêng theo loại người gửi (khách xám trung tính, nhân viên xanh, AI tím), và kèm nhãn người gửi phía trên nội dung từng tin (bỏ nhãn lặp lại phía dưới) để ai gửi nhìn là biết ngay. Confidence: 0.9
- Giao diện phải responsive đầy đủ trên mobile/tablet/desktop — khi người dùng nhắc "responsive của nút gửi tin nhắn chưa có", cần làm nút co lại thành icon trên màn hình hẹp (ẩn chữ, giữ kích thước chạm tối thiểu ~44px), giảm padding, input font ≥14px để tránh iOS zoom khi focus, và ẩn/thu hẹp list hội thoại trên màn hình nhỏ để chat chiếm ưu tiên toàn màn hình (dùng breakpoint xs/sm/md của MUI). Confidence: 0.8
- Muốn luồng gửi tin nhắn theo kiểu optimistic UI: khi bấm gửi, tin nhắn phải xuất hiện ngay trong hộp thoại kèm trạng thái trên từng tin — "Đang gửi..." (spinner), thành công thì thay bằng tin thật từ server, lỗi thì hiện "Không gửi được tin nhắn" + nút Thử lại giữ nguyên nội dung — thay vì chỉ hiển thị spinner trên nút gửi và chờ server phản hồi. Confidence: 0.8
- Mọi thao tác xóa dữ liệu quan trọng (như Page, hội thoại, hoặc bất kỳ thao tác không thể hoàn tác dễ dàng) bắt buộc phải có dialog xác nhận trước khi xóa — không được xóa ngay lập tức khi bấm nút, tránh trường hợp bấm lộn; áp dụng cho mọi thao tác xóa quan trọng trong ứng dụng. Confidence: 0.95
- Khi triển khai tính năng, phải tuân theo quy trình chặt chẽ 5 bước: (1) viết spec đầy đủ trước, (2) tự verify spec, (3) tự review spec và bổ sung nếu còn thiếu, (4) chỉ sau khi spec hoàn chỉnh mới chuyển sang design và lặp lại quy trình verify/review/bổ sung tương tự cho design, (5) chỉ khi có todo hoàn chỉnh, đầy đủ, không thiếu sót mới bắt đầu code. Confidence: 0.98
- Với agent AI tra cứu thông tin (sản phẩm/đơn hàng/FAQ), muốn nối tool vào dữ liệu thật trong DB (thêm model Prisma + truy vấn thật + seed dữ liệu mẫu) thay vì để tool stub trả chuỗi cứng — đã chốt hướng "agent tra cứu thật" thay vì chỉ hoàn thiện khung; mọi tool call nên được ghi log audit (AgentLog). Confidence: 0.75
- Luồng AI trả lời khách giữ tự động, không thêm bước phê duyệt thủ công của nhân viên; an toàn mặc định bằng cách escalate khi không chắc chắn hoặc thiếu điều kiện (như không có OPENAI_API_KEY thì chặn nhánh LLM, nhưng rule template vẫn chạy được) — không làm chậm luồng trả lời. Confidence: 0.8
- Sau khi đã liệt kê đầy đủ các trường hợp xử lý của một luồng (kể cả edge case), muốn bước triển khai tiếp theo bao phủ toàn bộ các trường hợp đó để hệ thống thực sự xử lý được hết — không chỉ làm luồng chính; người dùng ra chỉ thị "dựa trên toàn bộ các trường hợp trên bắt đầu làm để có thể xử lý được các trường hợp trên" (tương tự sự đầy đủ ở bước liệt kê, áp dụng cả ở bước triển khai). Confidence: 0.6
