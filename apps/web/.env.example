# API base URL (REST). Leave empty to use the Vite dev proxy (/api).
VITE_API_URL=https://omni-api.ttamdev.tech
# Socket.IO server URL. Leave empty to connect to same origin.
VITE_WS_URL=https://omni-api.ttamdev.tech
